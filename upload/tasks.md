# Список задач для разработки

**Дата:** 23 декабря 2024  
**Статус:** Требуется разработка

---

## 🔴 КРИТИЧЕСКИЕ ИСПРАВЛЕНИЯ

### 1. Отображение разовых платежей в календаре
**Проблема:** Разовые платежи (`frequency: 'once'`) не отображаются в календаре дашборда  
**Файл:** `src/components/budget/views/DashboardView.tsx`  
**Решение:** Обновить `calculateForecast` для учета разовых платежей с `targetMonth` и `targetYear`

### 2. Сброс статусов при переходе на новый месяц
**Проблема:** Расходы и доходы, отмеченные как совершенные/полученные, остаются отмеченными в следующем месяце  
**Файлы:** 
- `src/store/budgetStore.ts` (добавить метод сброса статусов)
- `src/components/budget/views/IncomeView.tsx` (обновить логику отображения)
- `src/components/budget/views/ExpensesView.tsx` (обновить логику отображения)

**Требования:**
- При переходе на новый месяц автоматически сбрасывать статус "получен" для всех доходов
- При переходе на новый месяц автоматически сбрасывать статус "оплачен" для всех расходов
- Сброс должен происходить при изменении месяца в настройках или при первом открытии нового месяца
- Для разовых платежей (`frequency: 'once'`) статус должен сбрасываться только если текущий месяц не совпадает с `targetMonth`
- **ВАЖНО:** Расходы и доходы, отмеченные как совершенные в следующем месяце (будущем), НЕ должны помечаться как совершенные
- **ВАЖНО:** При попытке пометить транзакцию как совершенную необходимо проверять дату транзакции - если она относится к будущему месяцу, статус не должен устанавливаться
- **ВАЖНО:** Транзакции из будущего месяца должны автоматически сбрасываться в статус "не совершено" при загрузке данных

**Технические детали:**
- Добавить метод `resetMonthlyStatuses(currentMonth: number, currentYear: number)` в store
- Добавить метод `canMarkAsCompleted(transaction: Income | Expense, currentMonth: number, currentYear: number): boolean` для проверки возможности установки статуса
- Вызывать метод при изменении месяца в настройках
- Проверять при загрузке данных, если месяц изменился - выполнять сброс
- При установке статуса "совершено" проверять дату транзакции относительно текущего месяца
- Для периодических транзакций проверять дату следующего выполнения относительно текущего месяца
- Для разовых платежей проверять `targetMonth` и `targetYear` относительно текущего месяца

---

### 3. Неправильный расчет расходов в обзоре и аналитике
**Проблема:** В разделе "Обзор" показывается "Расходы за месяц: 0 ₽", а в разделе "Аналитика" показывается "Всего расходов: 0 ₽", хотя есть расходы на сумму 43 749 ₽ (Кредит: 15 000 ₽, Электроэнергия: 3 500 ₽, Продукты: 25 000 ₽, Подписка: 249 ₽)  
**Файлы:**
- `src/store/budgetStore.ts` (метод расчета расходов за месяц)
- `src/components/budget/views/DashboardView.tsx` (отображение расходов в обзоре)
- `src/components/budget/views/AnalyticsView.tsx` (отображение расходов в аналитике)

**Требования:**
- Исправить расчет расходов за месяц - учитывать все расходы, независимо от статуса оплаты
- В разделе "Обзор" показывать правильную сумму расходов за месяц
- В разделе "Аналитика" показывать правильную сумму всех расходов
- Убедиться, что расчет учитывает все типы расходов (обязательные и необязательные)

**Технические детали:**
- Проверить метод `calculateMonthlyExpenses()` в store
- Убедиться, что метод суммирует все расходы из массива `expenses`, а не только оплаченные
- Проверить фильтрацию расходов по месяцу - возможно, проблема в определении принадлежности расхода к месяцу

---

### 4. Расходы не отображаются в календаре
**Проблема:** В календаре движения средств отображается только доход на 10 число (100 000 ₽), но не отображаются расходы на 15 число (Кредит: 15 000 ₽), 20 число (Электроэнергия: 3 500 ₽) и 5 число (Подписка: 249 ₽)  
**Файлы:**
- `src/components/budget/views/DashboardView.tsx` (компонент календаря)
- `src/store/budgetStore.ts` (метод `calculateForecast` для расчета транзакций по дням)

**Требования:**
- Все расходы должны отображаться в календаре в соответствующие дни месяца
- Расходы должны визуально отличаться от доходов (например, другим цветом или иконкой)
- При клике на день с расходами должна отображаться информация о всех транзакциях этого дня
- Разовые расходы должны отображаться только в указанный месяц (`targetMonth`, `targetYear`)

**Технические детали:**
- Проверить метод `calculateForecast` - убедиться, что он учитывает расходы
- Проверить компонент календаря - убедиться, что он отображает расходы
- Добавить визуальное различие между доходами и расходами в календаре

---

## 🟡 НОВЫЙ ФУНКЦИОНАЛ

### 5. Начальный баланс для новых пользователей
**Описание:** Возможность указать текущий баланс при первом запуске  
**Файлы:** 
- `src/components/onboarding/InitialBalanceDialog.tsx` (новый)
- `src/store/budgetStore.ts` (добавить `initialBalance` в settings)

**Требования:**
- Модальное окно при первом запуске (`settings.isFirstRun`)
- Поле ввода баланса (число, может быть отрицательным)
- Сохранение в `settings.initialBalance`
- Учет в расчете баланса

---

### 6. Управление категориями
**Описание:** Создание, редактирование, удаление пользовательских категорий  
**Файлы:**
- `src/components/categories/CategoriesManager.tsx` (новый)
- `src/types/budget.ts` (добавить `Category`)
- `src/store/budgetStore.ts` (методы для работы с категориями)

**Требования:**
- Раздел "Категории" в настройках или отдельная страница
- Создание категорий для доходов и расходов
- Редактирование названия, цвета, иконки
- Удаление с проверкой использования
- Предустановленные категории редактируемы

---

### 7. Учет займов
**Описание:** Функционал для учета взятых и выданных займов  
**Файлы:**
- `src/components/loans/LoansView.tsx` (новый)
- `src/types/budget.ts` (добавить `Loan`)
- `src/store/budgetStore.ts` (методы для работы с займами)

**Требования:**
- Типы: "Взял в долг" / "Дал в долг"
- Поля: сумма, дата получения/выдачи, дата возврата, процент (опционально)
- Статусы: Активный, Возвращен, Просрочен
- Учет в прогнозе кассовых разрывов
- Уведомления о приближающихся датах возврата

---

### 8. Система хранения и синхронизации
**Описание:** Supabase + локальное хранилище с синхронизацией  
**Документ:** [TZ-storage-sync-security.md](TZ-storage-sync-security.md)  
**Приоритет:** Высокий

**Основные задачи:**
- Настройка Supabase проекта
- Реализация HybridStorage (IndexedDB + Supabase)
- Авторизация через Supabase Auth
- Совместный доступ к данным
- Защита от подмены токенов (RLS)

---

## ⚠️ МЕЛКИЕ ИСПРАВЛЕНИЯ

### 9. Предупреждение доступности диалога
**Проблема:** `Warning: Missing Description or aria-describedby` для DialogContent  
**Файл:** Форма "Добавить доход"  
**Решение:** Добавить `DialogDescription` компонент

### 10. Ошибки загрузки шрифтов (404)
**Проблема:** Шрифты не загружаются  
**Файлы:** `4cf2300e9c8272f7-s.p.woff2`, `93f479601ee12b01-s.p.woff2`  
**Решение:** Исправить пути к шрифтам или использовать fallback

---

## 📋 ПРИОРИТЕТЫ

1. **Высокий:** Задачи 1, 2, 3, 4, 8
2. **Средний:** Задачи 5, 6, 7
3. **Низкий:** Задачи 9, 10

---

**Всего задач:** 10  
**Критических:** 4  
**Новых функций:** 4  
**Мелких исправлений:** 2

---

## 📝 ЗАМЕТКИ ИЗ ИССЛЕДОВАНИЯ (23 декабря 2024)

**Исследовано через Chrome MCP:**
- ✅ Форма добавления дохода работает корректно, поддерживает разовые платежи с полями "Целевой месяц" и "Год"
- ✅ Форма добавления расхода присутствует (не проверялась детально)
- ✅ Пометка расходов как оплаченных работает (статус меняется с "Не оплачено" на "Оплачено")
- ⚠️ **Проблема:** Расходы не учитываются в расчете "Расходы за месяц" (показывается 0 ₽ вместо 43 749 ₽)
- ⚠️ **Проблема:** Расходы не отображаются в календаре движения средств
- ⚠️ **Проблема:** В аналитике показывается "Всего расходов: 0 ₽" при наличии 4 платежей
- ⚠️ **Предупреждение:** Missing Description для DialogContent (доступность)

