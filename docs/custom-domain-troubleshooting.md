# Диагностика проблемы с кастомным доменом

## 🔍 Результаты анализа через Chrome MCP

### ✅ Vercel.app домен (работает):
- URL: `https://homeaccounting.vercel.app/`
- **bodyChildren: 13** - полный контент рендерится
- **bodyContent:** содержит полный HTML с приложением
- Страница полностью функциональна

### ❌ Кастомный домен (не работает):
- URL: `https://www.homeaccounting.ru/`
- **bodyChildren: 4** - только базовые элементы
- **bodyContent:** только пустые комментарии `<!--$--><!--/$-->`
- Скрипты загружаются (10 скриптов), но контент не рендерится
- SSL работает (`protocol: https:`)

## 🔴 Проблема

Скрипты Next.js загружаются, но React не инициализируется и контент не рендерится на кастомном домене.

## ✅ Решения для проверки

### 1. Проверить настройки домена в Vercel

1. Откройте Vercel Dashboard
2. Перейдите в проект → **Settings** → **Domains**
3. Проверьте что домен `homeaccounting.ru` добавлен и имеет статус **Valid**
4. Проверьте что `www.homeaccounting.ru` также добавлен (если используется)

### 2. Проверить DNS записи

Убедитесь что DNS записи настроены правильно:

**Для домена без www (homeaccounting.ru):**
```
Type: A
Name: @
Value: 76.76.21.21 (Vercel IP)
```

**Для домена с www (www.homeaccounting.ru):**
```
Type: CNAME
Name: www
Value: cname.vercel-dns.com
```

Или используйте A-запись:
```
Type: A
Name: www
Value: 76.76.21.21
```

### 3. Проверить SSL сертификат

1. В Vercel Dashboard → Settings → Domains
2. Убедитесь что SSL сертификат выдан (может занять до 24 часов)
3. Статус должен быть **Valid** или **Pending**

### 4. Очистить кеш Vercel

1. В Vercel Dashboard → Deployments
2. Найдите последний deployment
3. Нажмите **Redeploy** (три точки → Redeploy)

### 5. Проверить Environment Variables

Убедитесь что все переменные окружения установлены для production:
- `NEXT_PUBLIC_SUPABASE_URL`
- `NEXT_PUBLIC_SUPABASE_ANON_KEY`

### 6. Проверить Redirects (если есть)

Если у вас есть `vercel.json` или настройки redirects, убедитесь что они не конфликтуют с кастомным доменом.

### 7. Временное решение

Пока проблема не решена, можно использовать:
- `https://homeaccounting.vercel.app` - работает отлично
- Или настроить редирект с кастомного домена на vercel.app

## 🔧 Дополнительные проверки

### Проверить через curl:

```bash
# Проверить заголовки ответа
curl -I https://www.homeaccounting.ru/

# Проверить что возвращается
curl https://www.homeaccounting.ru/ | head -50
```

### Проверить DNS:

```bash
# Проверить A-запись
dig homeaccounting.ru A

# Проверить CNAME для www
dig www.homeaccounting.ru CNAME
```

## 📝 Что проверить в первую очередь

1. ✅ **DNS записи** - самая частая причина
2. ✅ **SSL сертификат** - должен быть выдан Vercel
3. ✅ **Статус домена в Vercel** - должен быть Valid
4. ✅ **Redeploy** - пересобрать проект после изменений DNS

## 🚀 После исправления

После исправления DNS/SSL:
1. Подождать до 24 часов для распространения DNS
2. Очистить DNS кеш: `ipconfig /flushdns` (Windows) или `sudo dscacheutil -flushcache` (Mac)
3. Проверить сайт снова

