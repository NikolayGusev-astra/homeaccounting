# Список исправлений для разработчика

**Дата:** 24 декабря 2024  
**Версия приложения:** Preview  
**URL:** https://preview-chat-1392659d-556d-4700-9697-a5dad626001c.space.z.ai/  
**Репозиторий:** https://github.com/NikolayGusev-astra/homeaccounting

---

## 📋 КРАТКОЕ РЕЗЮМЕ

**Всего проблем:** 7  
**Критических:** 1  
**Высокий приоритет:** 2  
**Средний приоритет:** 4  
**Исправлено ранее:** 3  
**Статус после тестирования:** ❌ Критические проблемы НЕ ИСПРАВЛЕНЫ

**Дата последнего тестирования:** 24 декабря 2024  
**Результаты тестирования:** `docs/test-results-after-fixes.md`

---

## 🔴 КРИТИЧЕСКИЕ ИСПРАВЛЕНИЯ

### ИСПРАВЛЕНИЕ #1: Оплаченные расходы пропадают из обзора и раздела расходов

**Приоритет:** 🔴 КРИТИЧЕСКИЙ  
**Тип:** Баг  
**Дата обнаружения:** 24 декабря 2024

#### Описание проблемы
При выставлении статуса "оплачено" (`paid: true`) расход исчезает из:
- Раздела "Обзор" (DashboardView)
- Раздела "Расходы за месяц" (ExpensesView)
- Но остается в разделе "Аналитика" (AnalyticsView)

#### Ожидаемое поведение
Оплаченные расходы должны отображаться во всех разделах с визуальным отличием (зачеркнутый текст, серый цвет, иконка галочки), чтобы пользователь мог видеть историю платежей.

#### Воспроизведение
1. Открыть раздел "Обзор"
2. Найти расход в списке расходов за месяц
3. Отметить расход как "оплачено" (изменить статус `paid: false` → `paid: true`)
4. Расход исчезает из обзора и из раздела "Расходы"
5. Перейти в "Аналитика" - расход там отображается

#### Файлы для исправления

**1. `src/components/budget/views/DashboardView.tsx`**
- **Проблема:** Используется фильтр `expenses.filter(e => !e.paid)`, который исключает оплаченные расходы
- **Решение:** Убрать фильтр `filter(e => !e.paid)` из отображения расходов
- **Дополнительно:** Добавить визуальную индикацию оплаченных расходов (CSS класс для зачеркнутого текста или серого цвета)

**Пример исправления:**
```typescript
// БЫЛО:
const unpaidExpenses = expenses.filter(e => !e.paid);

// ДОЛЖНО БЫТЬ:
const allExpenses = expenses; // Показывать все расходы
// Добавить визуальную индикацию:
{expense.paid && <span className="line-through text-gray-500">...</span>}
```

**2. `src/components/budget/views/ExpensesView.tsx`**
- **Проблема:** Используется фильтр `expenses.filter(e => !e.paid)` для отображения расходов за месяц
- **Решение:** Убрать фильтр `filter(e => !e.paid)` из списка расходов за месяц
- **Дополнительно:** Добавить визуальную индикацию оплаченных расходов

**3. `src/store/budgetStore.ts`**
- **Проблема:** Метод `calculateMonthlyExpenses()` может фильтровать оплаченные расходы
- **Решение:** Проверить метод и убедиться, что он не фильтрует по `paid === false`
- **Дополнительно:** Убедиться, что оплаченные расходы учитываются в расчетах баланса

#### Технические детали
- Найти все места, где используется фильтр `expenses.filter(e => !e.paid)`
- Убрать фильтр из отображения расходов в обзоре
- Убрать фильтр из отображения расходов за месяц
- Убедиться, что оплаченные расходы учитываются в расчетах баланса
- Добавить визуальную индикацию оплаченных расходов:
  - Зачеркнутый текст: `className="line-through"`
  - Серый цвет: `className="text-gray-500"`
  - Иконка галочки: добавить иконку `✓` или `Check` компонент

#### Тестирование после исправления
1. Отметить расход как "оплачено"
2. Проверить, что расход остается видимым в разделе "Обзор"
3. Проверить, что расход остается видимым в разделе "Расходы"
4. Проверить, что расход отображается с визуальным отличием (зачеркнут/серый)
5. Проверить, что расход учитывается в расчетах баланса

#### ✅ РЕЗУЛЬТАТЫ ТЕСТИРОВАНИЯ (24 декабря 2024)
- ✅ Оплаченные расходы отображаются в разделе "Расходы"
- ✅ Оплаченные расходы учитываются в "Расходы за месяц"
- ❌ **ПРОБЛЕМА:** Оплаченные расходы НЕ отображаются в календаре
- ❌ **ПРОБЛЕМА:** При клике на дату оплаченного расхода диалог показывает "Нет платежей на этот день"

**Вывод:** Проблема частично исправлена - расходы остаются видимыми в разделе "Расходы", но не отображаются в календаре. Требуется дополнительное исправление метода `calculateForecast`.

---

## 🔴 ВЫСОКИЙ ПРИОРИТЕТ

### ИСПРАВЛЕНИЕ #2: Разовые платежи не отображаются в календаре

**Приоритет:** 🔴 ВЫСОКИЙ  
**Тип:** Баг  
**Задача:** #2

#### Описание проблемы
Разовые платежи (`frequency: 'once'`) с указанными `targetMonth` и `targetYear` не отображаются в календаре дашборда, даже если они добавлены корректно.

#### Ожидаемое поведение
- Разовые доходы должны отображаться в календаре в указанную дату целевого месяца
- Разовые расходы должны отображаться в календаре в указанную дату целевого месяца
- В календаре должна быть визуальная индикация разовых платежей (фиолетовые точки `bg-purple-500`)
- Разовые платежи должны учитываться в прогнозе баланса только для указанного месяца

#### Воспроизведение
1. Добавить разовый доход на 20 декабря 2024 (frequency: 'once', targetMonth: 12, targetYear: 2024)
2. Добавить разовый расход на 31 декабря 2024 (frequency: 'once', targetMonth: 12, targetYear: 2024)
3. Перейти в раздел "Обзор"
4. Прокрутить до календаря
5. **Проблема:** Фиолетовые точки не отображаются на датах 20 и 31 декабря

#### Файлы для исправления

**1. `src/store/budgetStore.ts` - метод `calculateForecast`**

**Проблема:** Метод не учитывает разовые платежи с `targetMonth` и `targetYear`

**Решение:** Обновить логику фильтрации для разовых платежей

**Пример исправления:**
```typescript
// В методе calculateForecast добавить проверку для разовых платежей:

// Для доходов:
incomes.forEach(income => {
  if (income.frequency === 'once') {
    // Для разовых платежей проверяем targetMonth и targetYear
    if (income.targetYear === year && income.targetMonth === month) {
      // Учитываем только в указанном месяце
      const day = income.day || 1;
      if (!forecast[day]) {
        forecast[day] = { incomes: [], expenses: [] };
      }
      forecast[day].incomes.push(income);
    }
  } else {
    // Для периодических платежей используем существующую логику
    // ... существующий код ...
  }
});

// Аналогично для расходов:
expenses.forEach(expense => {
  if (expense.frequency === 'once') {
    if (expense.targetYear === year && expense.targetMonth === month) {
      const day = expense.day || 1;
      if (!forecast[day]) {
        forecast[day] = { incomes: [], expenses: [] };
      }
      forecast[day].expenses.push(expense);
    }
  } else {
    // ... существующий код для периодических расходов ...
  }
});
```

**2. `src/components/budget/views/DashboardView.tsx`**

**Проблема:** Компонент календаря может не получать разовые платежи из store

**Решение:** Убедиться, что компонент получает все транзакции из `calculateForecast`, включая разовые

**Дополнительно:** Проверить визуальное отображение разовых платежей (фиолетовые точки)

**Пример проверки:**
```typescript
// В компоненте календаря убедиться, что разовые платежи отображаются:
{transaction.frequency === 'once' && (
  <div className="w-2 h-2 rounded-full bg-purple-500" />
)}
```

#### Технические детали
- Проверить метод `calculateForecast` - убедиться, что он учитывает разовые платежи
- Для разовых платежей проверять `targetMonth` и `targetYear` относительно текущего месяца
- Убедиться, что разовые платежи включаются в результат `calculateForecast`
- Проверить компонент календаря - убедиться, что он отображает разовые платежи
- Добавить визуальную индикацию разовых платежей (фиолетовые точки `bg-purple-500`)

#### Тестирование после исправления
1. Добавить разовый доход на 20 декабря 2024
2. Добавить разовый расход на 31 декабря 2024
3. Перейти в раздел "Обзор"
4. Проверить календарь - должны быть фиолетовые точки на 20 и 31 декабря
5. Кликнуть на даты - должны открываться диалоги с разовыми платежами

#### ✅ РЕЗУЛЬТАТЫ ТЕСТИРОВАНИЯ (24 декабря 2024)
- ✅ Форма добавления разовых платежей работает корректно
- ✅ Поля `targetMonth` и `targetYear` отображаются при выборе "Разово"
- ✅ Разовый доход успешно сохраняется
- ❌ **ПРОБЛЕМА:** Разовые платежи НЕ отображаются в календаре
- ❌ **ПРОБЛЕМА:** При клике на дату разового платежа диалог показывает "Нет платежей на этот день"
- ⚠️ **ДОПОЛНИТЕЛЬНАЯ ПРОБЛЕМА:** В списке доходов разовый доход отображается как "Раз в 2 недели" вместо "Разово"

**Вывод:** Проблема НЕ ИСПРАВЛЕНА. Требуется исправление метода `calculateForecast` и компонента календаря.

---

### ИСПРАВЛЕНИЕ #3: Сброс статусов при переходе на новый месяц

**Приоритет:** 🔴 ВЫСОКИЙ  
**Тип:** Баг  
**Задача:** #3

#### Описание проблемы
Расходы и доходы, отмеченные как совершенные/полученные, остаются отмеченными в следующем месяце. Это приводит к неправильному отображению статусов транзакций.

#### Ожидаемое поведение
- При переходе на новый месяц автоматически сбрасывать статус "получен" для всех доходов
- При переходе на новый месяц автоматически сбрасывать статус "оплачен" для всех расходов
- Сброс должен происходить при изменении месяца в настройках или при первом открытии нового месяца
- Для разовых платежей (`frequency: 'once'`) статус должен сбрасываться только если текущий месяц не совпадает с `targetMonth`
- **ВАЖНО:** Расходы и доходы, отмеченные как совершенные в будущем месяце, НЕ должны помечаться как совершенные
- **ВАЖНО:** При попытке пометить транзакцию как совершенную необходимо проверять дату транзакции - если она относится к будущему месяцу, статус не должен устанавливаться
- **ВАЖНО:** Транзакции из будущего месяца должны автоматически сбрасываться в статус "не совершено" при загрузке данных

#### Файлы для исправления

**1. `src/store/budgetStore.ts`**

**Решение:** Добавить методы для сброса статусов и проверки возможности установки статуса

**Пример реализации:**
```typescript
// Добавить метод для проверки возможности установки статуса
canMarkAsCompleted(
  transaction: Income | Expense, 
  currentMonth: number, 
  currentYear: number
): boolean {
  // Для разовых платежей проверяем targetMonth и targetYear
  if (transaction.frequency === 'once') {
    const targetMonth = (transaction as any).targetMonth;
    const targetYear = (transaction as any).targetYear;
    
    // Нельзя пометить как совершенное, если это будущий месяц
    if (targetYear > currentYear || 
        (targetYear === currentYear && targetMonth > currentMonth)) {
      return false;
    }
    
    // Можно пометить только если это текущий или прошедший месяц
    return targetYear === currentYear && targetMonth === currentMonth;
  }
  
  // Для периодических платежей проверяем дату следующего выполнения
  // ... логика для периодических платежей ...
  
  return true;
}

// Добавить метод для сброса статусов
resetMonthlyStatuses(currentMonth: number, currentYear: number): void {
  // Сброс статусов доходов
  this.incomes.forEach(income => {
    if (income.frequency === 'once') {
      const targetMonth = (income as any).targetMonth;
      const targetYear = (income as any).targetYear;
      
      // Сбрасываем только если это не текущий месяц разового платежа
      if (!(targetYear === currentYear && targetMonth === currentMonth)) {
        income.received = false;
      }
    } else {
      // Для периодических платежей сбрасываем всегда
      income.received = false;
    }
  });
  
  // Аналогично для расходов
  this.expenses.forEach(expense => {
    if (expense.frequency === 'once') {
      const targetMonth = (expense as any).targetMonth;
      const targetYear = (expense as any).targetYear;
      
      if (!(targetYear === currentYear && targetMonth === currentMonth)) {
        expense.paid = false;
      }
    } else {
      expense.paid = false;
    }
  });
  
  this.save();
}
```

**2. `src/components/budget/views/IncomeView.tsx`**
- Вызывать `resetMonthlyStatuses` при изменении месяца
- Использовать `canMarkAsCompleted` перед установкой статуса

**3. `src/components/budget/views/ExpensesView.tsx`**
- Вызывать `resetMonthlyStatuses` при изменении месяца
- Использовать `canMarkAsCompleted` перед установкой статуса

#### Технические детали
- Добавить метод `resetMonthlyStatuses(currentMonth: number, currentYear: number)` в store
- Добавить метод `canMarkAsCompleted(transaction: Income | Expense, currentMonth: number, currentYear: number): boolean` для проверки возможности установки статуса
- Вызывать метод при изменении месяца в настройках
- Проверять при загрузке данных, если месяц изменился - выполнять сброс
- При установке статуса "совершено" проверять дату транзакции относительно текущего месяца
- Для периодических транзакций проверять дату следующего выполнения относительно текущего месяца
- Для разовых платежей проверять `targetMonth` и `targetYear` относительно текущего месяца

#### Тестирование после исправления
1. Отметить расход как "оплачено" в текущем месяце
2. Перейти на следующий месяц
3. Проверить, что статус сброшен (расход не оплачен)
4. Попытаться отметить транзакцию из будущего месяца как совершенную
5. Проверить, что статус не устанавливается

---

## 🟡 СРЕДНИЙ ПРИОРИТЕТ

### ИСПРАВЛЕНИЕ #4: Бейджи "Разовый платёж" не отображаются в детальном диалоге

**Приоритет:** 🟡 СРЕДНИЙ  
**Тип:** Баг

#### Описание проблемы
В детальном диалоге при клике на дату в календаре может отсутствовать бейдж "Разовый платёж" для разовых транзакций.

#### Ожидаемое поведение
- При клике на дату с разовым платежом должен отображаться бейдж "Разовый платёж"
- Зелёный бейдж для разовых доходов
- Фиолетовый бейдж для разовых расходов

#### Файлы для исправления

**1. `src/components/budget/views/DashboardView.tsx`**

**Проблема:** Компонент диалога может не проверять `frequency === 'once'`

**Решение:** Добавить проверку и отображение бейджа для разовых платежей

**Пример исправления:**
```typescript
// В компоненте диалога добавить проверку:
{transaction.frequency === 'once' && (
  <span className={`px-2 py-1 rounded text-xs ${
    transaction.type === 'income' 
      ? 'bg-green-500 text-white' 
      : 'bg-purple-500 text-white'
  }`}>
    Разовый платёж
  </span>
)}
```

#### Тестирование после исправления
1. Добавить разовый доход на 20 декабря
2. Добавить разовый расход на 31 декабря
3. Кликнуть на 20 декабря в календаре
4. Проверить наличие зелёного бейджа "Разовый платёж"
5. Кликнуть на 31 декабря в календаре
6. Проверить наличие фиолетового бейджа "Разовый платёж"

---

### ИСПРАВЛЕНИЕ #5: Разовые расходы могут не учитываться в "Расходы за месяц"

**Приоритет:** 🟡 СРЕДНИЙ  
**Тип:** Баг

#### Описание проблемы
Разовые расходы могут не учитываться в блоке "Расходы за месяц" в разделе "Обзор".

#### Ожидаемое поведение
- Разовые расходы должны учитываться в "Расходы за месяц" для указанного месяца
- Сумма должна включать все расходы текущего месяца, включая разовые

#### Файлы для исправления

**1. `src/store/budgetStore.ts` - метод `calculateMonthlyExpenses()`**

**Проблема:** Метод может не учитывать разовые расходы с `targetMonth` и `targetYear`

**Решение:** Обновить логику расчета для учета разовых расходов

**Пример исправления:**
```typescript
calculateMonthlyExpenses(month: number, year: number): number {
  return this.expenses
    .filter(expense => {
      // Для разовых платежей проверяем targetMonth и targetYear
      if (expense.frequency === 'once') {
        const targetMonth = (expense as any).targetMonth;
        const targetYear = (expense as any).targetYear;
        return targetYear === year && targetMonth === month;
      }
      
      // Для периодических платежей используем существующую логику
      // ... существующий код ...
    })
    .reduce((sum, expense) => sum + expense.amount, 0);
}
```

#### Тестирование после исправления
1. Добавить разовый расход на 31 декабря 2024 (30 000 ₽)
2. Перейти в раздел "Обзор"
3. Проверить блок "Расходы за месяц"
4. Убедиться, что сумма включает разовый расход

---

### ИСПРАВЛЕНИЕ #6: Неправильный расчет расходов в обзоре и аналитике

**Приоритет:** 🟡 СРЕДНИЙ  
**Тип:** Требует уточнения  
**Задача:** #4

#### Описание проблемы
В разделе "Обзор" показывается "Расходы за месяц: 18 500 ₽" (только обязательные), а в разделе "Расходы" видно:
- Обязательные платежи: 18 500 ₽
- Необязательные расходы: 25 249 ₽
- Итого: 43 749 ₽

#### Требуется уточнение
- Должна ли общая статистика в обзоре и аналитике показывать все расходы или только обязательные?
- Если только обязательные - это задуманное поведение, но нужно добавить отдельную строку для необязательных расходов
- Если все расходы - нужно исправить расчет, чтобы учитывались все типы расходов

#### Файлы для исправления

**1. `src/store/budgetStore.ts` - метод `calculateMonthlyExpenses()`**
- Проверить метод и убедиться, что он суммирует все расходы из массива `expenses`, а не только обязательные
- Проверить фильтрацию расходов по месяцу - возможно, проблема в определении принадлежности расхода к месяцу

**2. `src/components/budget/views/DashboardView.tsx`**
- Проверить отображение расходов в обзоре
- Если нужно показывать только обязательные - добавить отдельную строку для необязательных

**3. `src/components/budget/views/AnalyticsView.tsx`**
- Проверить отображение расходов в аналитике
- Убедиться, что логика расчета соответствует требованиям

---

### ИСПРАВЛЕНИЕ #7: Некорректное отображение периодичности разовых платежей

**Приоритет:** 🟡 СРЕДНИЙ  
**Тип:** Баг  
**Дата обнаружения:** 24 декабря 2024 (при тестировании)

#### Описание проблемы
В списке доходов разовый доход отображается как "Раз в 2 недели" вместо "Разово", хотя при добавлении была выбрана периодичность "Разово".

#### Воспроизведение
1. Добавить разовый доход с периодичностью "Разово"
2. Проверить отображение в списке доходов
3. **Проблема:** Отображается "Раз в 2 недели" вместо "Разово"

#### Файлы для исправления

**1. `src/components/budget/views/IncomeView.tsx`**

**Проблема:** Логика отображения периодичности некорректно обрабатывает разовые платежи

**Решение:** Проверить и исправить логику отображения периодичности для разовых платежей

**Пример исправления:**
```typescript
// Проверить функцию отображения периодичности
const getFrequencyLabel = (income: Income) => {
  if (income.frequency === 'once') {
    return 'Разово';
  }
  // ... остальная логика ...
};
```

#### Тестирование после исправления
1. Добавить разовый доход
2. Проверить отображение в списке доходов
3. Убедиться, что отображается "Разово"

---

## ✅ УЖЕ ИСПРАВЛЕНО

### ✅ ИСПРАВЛЕНО #1: Раздел "Расходы" не работает из-за отсутствия импорта useState
**Статус:** ✅ ИСПРАВЛЕНО  
**Файл:** `src/components/budget/views/ExpensesView.tsx`  
**Решение:** Добавлен импорт `useState` из React

### ✅ ИСПРАВЛЕНО #2: Расходы не отображаются в календаре
**Статус:** ✅ ИСПРАВЛЕНО (частично)  
**Файлы:** `src/components/budget/views/DashboardView.tsx`, `src/store/budgetStore.ts`  
**Примечание:** Расходы на 15 и 20 число отображаются, но разовые платежи все еще не отображаются

### ✅ ИСПРАВЛЕНО #3: Ошибка Hydration в DashboardLayout
**Статус:** ✅ ИСПРАВЛЕНО  
**Файл:** `src/components/budget/DashboardLayout.tsx`  
**Решение:** Добавлена проверка `isMounted` для клиентского рендеринга

---

## 📋 ЧЕКЛИСТ ДЛЯ РАЗРАБОТЧИКА

### Критические исправления (выполнить в первую очередь):
- [ ] **ИСПРАВЛЕНИЕ #1:** Убрать фильтр `filter(e => !e.paid)` из DashboardView и ExpensesView
- [ ] **ИСПРАВЛЕНИЕ #1:** Добавить визуальную индикацию оплаченных расходов
- [ ] **ИСПРАВЛЕНИЕ #2:** Обновить `calculateForecast` для учета разовых платежей
- [ ] **ИСПРАВЛЕНИЕ #2:** Добавить визуальную индикацию разовых платежей в календаре (фиолетовые точки)
- [ ] **ИСПРАВЛЕНИЕ #3:** Добавить метод `resetMonthlyStatuses` в store
- [ ] **ИСПРАВЛЕНИЕ #3:** Добавить метод `canMarkAsCompleted` в store
- [ ] **ИСПРАВЛЕНИЕ #3:** Вызывать `resetMonthlyStatuses` при изменении месяца

### Средние исправления:
- [ ] **ИСПРАВЛЕНИЕ #4:** Добавить бейджи "Разовый платёж" в детальный диалог
- [ ] **ИСПРАВЛЕНИЕ #5:** Обновить `calculateMonthlyExpenses` для учета разовых расходов
- [ ] **ИСПРАВЛЕНИЕ #6:** Уточнить требования и исправить расчет расходов
- [ ] **ИСПРАВЛЕНИЕ #7:** Исправить отображение периодичности разовых платежей в списке доходов

### Тестирование после исправлений:
- [ ] Проверить отображение оплаченных расходов в обзоре
- [ ] Проверить отображение разовых платежей в календаре
- [ ] Проверить сброс статусов при переходе на новый месяц
- [ ] Проверить отображение бейджей в детальном диалоге
- [ ] Проверить учет разовых расходов в "Расходы за месяц"

---

## 📝 ПРИМЕЧАНИЯ

1. Все исправления должны быть протестированы после внесения изменений
2. При исправлении ИСПРАВЛЕНИЯ #1 необходимо убедиться, что оплаченные расходы учитываются в расчетах баланса
3. При исправлении ИСПРАВЛЕНИЯ #2 необходимо проверить работу для разных месяцев и годов
4. При исправлении ИСПРАВЛЕНИЯ #3 необходимо учесть разовые платежи с `targetMonth` и `targetYear`
5. ИСПРАВЛЕНИЕ #6 требует уточнения требований у заказчика перед началом разработки
6. **ВАЖНО:** После исправления ИСПРАВЛЕНИЯ #1 и #2 необходимо также исправить отображение в календаре - оплаченные расходы и разовые платежи должны отображаться в диалоге календаря

## 📊 РЕЗУЛЬТАТЫ ТЕСТИРОВАНИЯ (24 декабря 2024)

**Подробный отчет:** `docs/test-results-after-fixes.md`

### Статус исправлений:
- ❌ **ИСПРАВЛЕНИЕ #1:** Частично исправлено - расходы остаются видимыми в разделе "Расходы", но НЕ отображаются в календаре
- ❌ **ИСПРАВЛЕНИЕ #2:** НЕ ИСПРАВЛЕНО - разовые платежи не отображаются в календаре
- ⚠️ **ИСПРАВЛЕНИЕ #3-6:** Требуют дополнительной проверки
- ✅ **ИСПРАВЛЕНИЕ #7:** Обнаружена новая проблема при тестировании

### Критические проблемы:
**Обе критические проблемы (#1 и #2) требуют дополнительного исправления метода `calculateForecast` для корректного отображения в календаре.**

---

**Дата составления:** 24 декабря 2024  
**Дата последнего обновления:** 24 декабря 2024 (после тестирования)  
**Статус:** ❌ Критические проблемы НЕ ИСПРАВЛЕНЫ - требуется дополнительная работа  
**Приоритет выполнения:** Критические → Высокий → Средний

