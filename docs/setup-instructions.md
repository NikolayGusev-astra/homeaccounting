# Инструкция: Настройка GitHub и VK OAuth + Перевод на английский

## 📋 ЧТО НУЖНО СДЕЛАТЬ ВАМ:

### 1. Настройка GitHub OAuth

1. **Откройте GitHub Settings:**
   - Перейдите: https://github.com/settings/developers
   - Войдите в свой аккаунт GitHub

2. **Создайте OAuth App:**
   - Нажмите **"OAuth Apps"** → **"New OAuth App"**
   - Заполните форму:
     - **Application name:** `Home Accounting`
     - **Homepage URL:** `https://homeaccounting.ru`
     - **Authorization callback URL:**
       ```
       https://yzdpqzjwqxzictfhcehk.supabase.co/auth/v1/callback
       ```
   - Нажмите **"Register application"**

3. **Скопируйте данные:**
   - **Client ID** - скопируйте и сохраните
   - Нажмите **"Generate a new client secret"**
   - **Client Secret** - скопируйте и сохраните (⚠️ показывается только один раз!)

4. **Добавьте в Supabase:**
   - Откройте: https://supabase.com/dashboard
   - Выберите проект: `yzdpqzjwqxzictfhcehk`
   - Перейдите: **Authentication** → **Providers** → **GitHub**
   - Включите провайдер (переключите тумблер в ON)
   - Вставьте **Client ID** и **Client Secret** из шага 3
   - Нажмите **Save**

---

### 2. Настройка VK OAuth

1. **Создайте приложение VK:**
   - Перейдите: https://vk.com/editapp?act=create
   - Войдите в свой аккаунт VK
   - Заполните форму:
     - **Название приложения:** `Home Accounting`
     - **Тип приложения:** Выберите **"Веб-сайт"**
     - **Адрес сайта:** `https://homeaccounting.ru`
     - **Базовый домен:** `homeaccounting.ru`
   - Нажмите **"Подключить приложение"**

2. **Настройте параметры приложения:**
   - После создания перейдите в настройки приложения
   - Найдите раздел **"Настройки"**
   - В поле **"Доверенный redirect URI"** добавьте:
     ```
     https://yzdpqzjwqxzictfhcehk.supabase.co/auth/v1/callback
     ```
   - Сохраните изменения

3. **Получите данные:**
   - **ID приложения** - найдите в настройках приложения
   - **Защищённый ключ** (Client Secret) - найдите в разделе **"Настройки"** → **"Защищённый ключ"**
   - Скопируйте и сохраните оба значения

4. **⚠️ ВАЖНО для VK:**
   - Supabase не поддерживает VK напрямую как OAuth провайдер
   - VK OAuth будет работать через VK SDK (уже реализовано в коде)
   - После получения ID и Secret, сообщите мне - я добавлю их в код

---

## ✅ ЧТО УЖЕ СДЕЛАНО В КОДЕ:

### 1. Убран переключатель темы
- ✅ Удалена функция `toggleTheme`
- ✅ Удалены кнопки переключения темы
- ✅ Удалены импорты `Moon` и `Sun`
- ✅ Приложение всегда использует темную тему

### 2. Создана система переводов (i18n)
- ✅ Создан файл `src/lib/i18n.ts`
- ✅ Поддержка русского и английского языков
- ✅ По умолчанию установлен английский язык
- ✅ Переводы сохраняются в localStorage

### 3. Начато внедрение переводов
- ✅ DashboardLayout переведен на использование `t()` функции
- ✅ Навигация переведена
- ✅ Основные кнопки переведены
- 🔄 Остальные компоненты будут переведены дальше

### 4. GitHub OAuth
- ✅ Функция `signInWithGitHub()` уже реализована
- ✅ После настройки в Supabase будет работать автоматически

### 5. VK OAuth (частично)
- ✅ Структура для VK OAuth готова
- ⏳ Требуется добавление VK SDK после получения credentials

---

## 🔧 ПОСЛЕ НАСТРОЙКИ:

1. **Проверьте GitHub OAuth:**
   - Откройте приложение
   - Нажмите "Sign In with GitHub"
   - Должна произойти авторизация

2. **VK OAuth:**
   - После получения VK credentials, сообщите мне
   - Я добавлю VK SDK и кнопку входа

---

## 📝 ПРИМЕЧАНИЯ:

- **GitHub OAuth** работает через Supabase напрямую
- **VK OAuth** требует VK SDK (будет добавлен после получения credentials)
- Все redirect URI должны указывать на Supabase callback URL
- После настройки данные сохраняются в Supabase автоматически
- **Язык по умолчанию:** английский (можно изменить в коде)

---

## 🚀 СЛЕДУЮЩИЕ ШАГИ:

После того как вы настроите GitHub OAuth:
1. Сообщите мне - я проверю работу
2. После получения VK credentials - я добавлю VK OAuth
3. Продолжу перевод остальных компонентов на английский

