# Исправление проблемы с deployment на Vercel

## 🔴 Проблема

При открытии `https://www.homeaccounting.ru/` отображается только заголовок страницы, контент не рендерится.

## 🔍 Причина

В `next.config.ts` был установлен `output: "standalone"`, который предназначен для Docker deployment, а не для Vercel. Это приводит к тому, что:

1. Скрипты Next.js не загружаются правильно
2. Нет `__NEXT_DATA__` скрипта
3. React не может инициализироваться
4. Контент не рендерится

## ✅ Решение

Убрать `output: "standalone"` из `next.config.ts` для Vercel deployment.

## 🚀 Что нужно сделать

1. **Изменения уже внесены в код** - `output: "standalone"` удален из `next.config.ts`

2. **Пересобрать и задеплоить на Vercel:**
   ```bash
   git add next.config.ts
   git commit -m "Fix: убран output standalone для Vercel deployment"
   git push
   ```

3. **Vercel автоматически пересоберет проект** после push

4. **Проверить deployment:**
   - Дождаться завершения сборки в Vercel Dashboard
   - Проверить что домен `homeaccounting.ru` правильно настроен
   - Открыть сайт и проверить что контент рендерится

## 📝 Дополнительные проверки

Если проблема останется после пересборки:

1. **Проверить DNS настройки:**
   - Убедиться что A-запись указывает на Vercel IP
   - Проверить CNAME записи

2. **Проверить Vercel Domain настройки:**
   - В Vercel Dashboard → Settings → Domains
   - Убедиться что домен добавлен и верифицирован

3. **Проверить Environment Variables:**
   - Убедиться что все переменные окружения установлены
   - Особенно `NEXT_PUBLIC_SUPABASE_URL` и `NEXT_PUBLIC_SUPABASE_ANON_KEY`

4. **Проверить Build Logs:**
   - В Vercel Dashboard → Deployments → выбрать последний deployment
   - Проверить логи на наличие ошибок

## 🔗 Полезные ссылки

- [Vercel Next.js Configuration](https://vercel.com/docs/frameworks/nextjs)
- [Next.js Output Modes](https://nextjs.org/docs/app/api-reference/next-config-js/output)

