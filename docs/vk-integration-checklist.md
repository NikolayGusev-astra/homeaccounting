# Чеклист интеграции VK ID

## ✅ Что уже сделано:

1. ✅ VK ID SDK интегрирован в `src/app/layout.tsx`
2. ✅ Компонент `VKAuth` создан в `src/components/auth/VKAuth.tsx`
3. ✅ Функция `signInWithVK` создана в `src/lib/supabase.ts`
4. ✅ Виджет добавлен в `AuthDialog`
5. ✅ Credentials сохранены в `docs/oauth-credentials.md`

## 🔍 Проверка настроек VK приложения:

### В настройках VK приложения должно быть:

- ✅ **App ID:** 54409028
- ✅ **Базовый домен:** `homeaccounting.ru`
- ✅ **Доверенные Redirect URL:**
  - `https://homeaccounting.online`
  - `https://homeaccounting.vercel.app/`
- ✅ **Защищённый ключ:** сохранен
- ✅ **Сервисный ключ доступа:** сохранен

## 🧪 Тестирование:

### 1. Локальное тестирование:

```bash
# Запустите приложение локально
npm run dev
# или
bun dev
```

1. Откройте `http://localhost:3000`
2. Откройте диалог авторизации
3. Проверьте, что виджет VK ID отображается
4. Попробуйте авторизоваться через VK

### 2. Тестирование на Vercel:

1. Задеплойте приложение на Vercel
2. Откройте `https://homeaccounting.vercel.app`
3. Проверьте авторизацию через VK

### 3. Тестирование на продакшене:

1. Откройте `https://homeaccounting.online`
2. Проверьте авторизацию через VK

## 🔧 Возможные проблемы и решения:

### Проблема: Виджет VK ID не отображается

**Решение:**
- Проверьте, что скрипт VK ID SDK загружается (откройте DevTools → Network)
- Проверьте консоль браузера на наличие ошибок
- Убедитесь, что `window.VKIDSDK` доступен

### Проблема: Ошибка "Invalid redirect URL"

**Решение:**
- Убедитесь, что redirect URL в VK приложении совпадает с тем, что используется в коде
- Проверьте, что базовый домен правильно настроен

### Проблема: Ошибка авторизации в Supabase

**Решение:**
- Проверьте, что Supabase правильно настроен
- Проверьте структуру данных, которые возвращает VK ID SDK
- Убедитесь, что функция `signInWithVK` правильно обрабатывает токен

## 📝 Следующие шаги:

1. **Протестировать авторизацию** через VK на всех окружениях
2. **Проверить обработку ошибок** - убедиться, что все ошибки правильно обрабатываются
3. **Улучшить UX** - добавить индикаторы загрузки и сообщения об ошибках
4. **Добавить логирование** - для отладки проблем в продакшене

## 🔐 Безопасность:

- ✅ Credentials сохранены в `.gitignore`
- ⚠️ Убедитесь, что сервисный ключ доступа не используется в клиентском коде
- ⚠️ Защищённый ключ должен использоваться только на сервере (если нужен)

## 📚 Документация:

- [VK ID SDK Documentation](https://id.vk.com/about/business/ru/docs/vkid/vkid/)
- [VK API Documentation](https://dev.vk.com/ru/api)

