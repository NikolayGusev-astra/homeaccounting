# Настройка VK OAuth через Supabase Edge Functions

## ✅ Что уже сделано в коде:

1. ✅ Создана Edge Function: `supabase/functions/vk-auth/index.ts`
2. ✅ Добавлена функция `signInWithVK` в `src/lib/supabase.ts`
3. ✅ Создан компонент `VKAuth` в `src/components/auth/VKAuth.tsx`
4. ✅ Интегрирован VKAuth в `AuthDialog`

## ⚠️ ВАЖНО: Выберите способ настройки

- **Если можете установить CLI:** Используйте инструкции ниже
- **Если НЕ можете установить CLI:** Используйте инструкции из `docs/vk-edge-function-setup-no-cli.md` (настройка через веб-интерфейс)

## 📋 Что нужно сделать ВРУЧНУЮ (через CLI):

### Шаг 1: Установите Supabase CLI

```bash
npm install -g supabase
```

Или через другие менеджеры пакетов:
- **macOS:** `brew install supabase/tap/supabase`
- **Windows:** `scoop bucket add supabase https://github.com/supabase/scoop-bucket.git && scoop install supabase`
- **Linux:** Скачайте бинарник с https://github.com/supabase/cli/releases

### Шаг 2: Войдите в Supabase

```bash
supabase login
```

Откроется браузер для авторизации. Войдите в свой аккаунт Supabase.

### Шаг 3: Свяжите проект с Supabase

```bash
cd /home/astralinux.ru/ngusev/Документы/zaiacc
supabase link --project-ref yzdpqzjwqxzictfhcehk
```

### Шаг 4: Установите переменные окружения для Edge Function

Вам нужно получить **Service Role Key** из Supabase Dashboard:

1. Откройте: https://supabase.com/dashboard/project/yzdpqzjwqxzictfhcehk/settings/api
2. Найдите раздел **"Project API keys"**
3. Скопируйте **"service_role"** ключ (⚠️ секретный ключ!)

Затем установите секреты для Edge Function:

```bash
supabase secrets set SUPABASE_SERVICE_ROLE_KEY=ваш_service_role_ключ
```

**Примечание:** `SUPABASE_URL` уже доступен автоматически в Edge Functions, его не нужно устанавливать.

### Шаг 5: Деплой Edge Function

```bash
supabase functions deploy vk-auth
```

После успешного деплоя вы увидите URL функции, например:
```
https://yzdpqzjwqxzictfhcehk.supabase.co/functions/v1/vk-auth
```

### Шаг 6: Проверьте работу

1. Откройте приложение
2. Нажмите "Войти"
3. Должен появиться виджет VK ID
4. Попробуйте войти через VK

## 🔧 Отладка

Если что-то не работает:

1. **Проверьте логи Edge Function:**
   ```bash
   supabase functions logs vk-auth
   ```

2. **Проверьте переменные окружения:**
   ```bash
   supabase secrets list
   ```

3. **Проверьте консоль браузера** на наличие ошибок

4. **Проверьте Network tab** в DevTools - должен быть запрос к `/functions/v1/vk-auth`

## 📝 Примечания

- Edge Function использует **Service Role Key** для создания пользователей в Supabase
- VK токен обменивается на сессию Supabase через Edge Function
- Все данные пользователя сохраняются в `user_metadata`
- Email генерируется автоматически, если VK не предоставил его

## 🔒 Безопасность

- Service Role Key должен храниться только в секретах Supabase
- Никогда не коммитьте Service Role Key в git
- Edge Function проверяет валидность VK токена через VK API

