# Решение проблемы с кастомным доменом

## 🔍 Диагностика

### Результаты проверки:

1. **HTTP статус:** Оба домена возвращают `200 OK` ✅
2. **HTML контент:** Одинаковый (только skeleton) ✅
3. **Скрипты:** Загружаются на обоих доменах ✅
4. **Проблема:** JavaScript не выполняется на кастомном домене ❌

### Что работает:
- ✅ `https://homeaccounting.vercel.app` - полностью работает
- ❌ `https://www.homeaccounting.ru` - только заголовок, контент не рендерится

## 🔴 Вероятная причина

Проблема в **кешировании Vercel**. Старая версия (с `output: standalone`) закеширована для кастомного домена.

## ✅ Решение

### 1. Очистить кеш Vercel

**В Vercel Dashboard:**
1. Перейдите в проект → **Deployments**
2. Найдите последний deployment
3. Нажмите **⋮** (три точки) → **Redeploy**
4. Или создайте новый deployment через **Redeploy**

### 2. Проверить что изменения применены

После redeploy проверьте:
- Откройте `https://www.homeaccounting.ru/`
- Должен отображаться полный контент (как на vercel.app)

### 3. Если не помогло - очистить кеш браузера

```bash
# В браузере:
Ctrl+Shift+R (Windows/Linux)
Cmd+Shift+R (Mac)
```

Или откройте в режиме инкогнито.

### 4. Проверить DNS кеш (если проблема останется)

```bash
# Windows
ipconfig /flushdns

# Mac
sudo dscacheutil -flushcache

# Linux
sudo systemd-resolve --flush-caches
```

## 📝 Дополнительные проверки

### Проверить что deployment успешен:

1. В Vercel Dashboard → Deployments
2. Убедитесь что последний deployment имеет статус **Ready**
3. Проверьте Build Logs на наличие ошибок

### Проверить настройки домена:

1. Vercel Dashboard → Settings → Domains
2. Убедитесь что `homeaccounting.ru` и `www.homeaccounting.ru` имеют статус **Valid**
3. SSL сертификат должен быть выдан

## 🚀 После исправления

После redeploy подождите 1-2 минуты и проверьте сайт снова. Если проблема останется, возможно нужно подождать распространения DNS (до 24 часов).

## 💡 Временное решение

Пока проблема не решена, можно использовать:
- `https://homeaccounting.vercel.app` - работает отлично

Или настроить редирект в DNS:
```
Type: CNAME
Name: www
Value: homeaccounting.vercel.app
```

